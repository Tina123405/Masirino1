services:
  - type: web
    name: masirino
    runtime: python
    buildCommand: echo "No requirements needed"
    startCommand: python main.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0